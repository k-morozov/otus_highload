### How to run

Server listen:
127.0.0.1:8000

Server uses this path for connection to PostgreSQL:
"postgres://dev:123@localhost/app_db"

I assume that need to add config file or env but I think right now its enough :)